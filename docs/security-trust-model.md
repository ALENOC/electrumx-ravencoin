# Discovery and backend trust model — consolidated

This document has been consolidated into the maintained
[Security model](security-model.md), especially **Endpoint trust ladder** and
**Why the project does not add signed per-peer capability claims**.

The canonical security model now covers discovery, capability claims, signed
safe-Core policy, independent chain evidence, operator trust, and the reason a
peer signing its own self-report would not constitute remote attestation.

The original detailed working document remains recoverable from Git history.
